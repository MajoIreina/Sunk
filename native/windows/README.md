# Windows Native Integration

This directory is reserved for Sunk's Windows desktop-window and shell-integration source.
Generated native build output belongs in `native/windows/build` and is ignored.
